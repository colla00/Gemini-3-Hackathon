import { Navigate } from 'react-router-dom';

// Consolidated: IntegrationGuide content merged into Integrations (/integrations)
const IntegrationGuide = () => <Navigate to="/integrations" replace />;

export default IntegrationGuide;
