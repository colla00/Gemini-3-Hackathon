import { Navigate } from 'react-router-dom';

// Consolidated: Regulatory content merged into TrustCenter (/trust)
const Regulatory = () => <Navigate to="/trust" replace />;

export default Regulatory;
